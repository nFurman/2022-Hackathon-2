class Move {
  Move(side, piece, startRow, startCol, endRow, endTile) {
    this.side = side;
    this.piece = piece;
    this.startRow = startRow;
    this.startCol = startCol;
    this.endRow = endRow;
    this.endCol = endCol;
  }
}
